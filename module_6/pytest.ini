[pytest]
testpaths = tests
norecursedirs = .venv __pycache__ .git
markers =
    unit: unit tests
    integration: integration tests
    web: web/Flask page tests
    analysis: analysis tests
    buttons: UI/button tests
    db: database tests
